# Default ProGuard rules for the app. Compose, Hilt, and kotlinx.serialization
# come with sensible defaults that ship with each library; we keep this file
# intentionally minimal.
-keepattributes *Annotation*
-keepattributes Signature
